**`C:\Users\machi\inkwell-ai-2\.gitignore`** (Full content for your project root)
```
# Gitignore for the entire Inkwell AI project (monorepo style)

# Node.js modules for both frontend and backend
frontend/node_modules/
backend/node_modules/

# Environment variables (sensitive data, don't commit!)
.env
.env.local
.env.development
.env.production
frontend/.env*
backend/.env*

# Local development logs and caches
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store # macOS specific

# Backend specific ignores
backend/*.db
backend/*.db-shm
backend/*.db-wal
backend/serviceAccountKey.json # No longer needed in repo if using env var

# Frontend specific ignores
frontend/dist/ # Vite's build output directory
```
* **Save this `.gitignore` file.**